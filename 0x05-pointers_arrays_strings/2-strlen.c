#include "main.h"
/**
* _strlen(char *s) - Function that returns the length of a string
* @s: Parameter (the string you need to find the length for)
* Return: Returns the length of the string passed
*/
int _strlen(char *s)
{
	return strlen(s);
}
